rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // File bytes are server-authoritative. The browser uploads and resolves
    // through authenticated application APIs; Firebase Admin uses IAM and
    // bypasses these client rules.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
